# Runtime dependencies for the ge_pipeline package.
# Kept in sync with [project.dependencies] in pyproject.toml.
# For an editable install with test extras, prefer:
#   pip install -e ".[test]"
influxdb-client==1.50.0
numpy==2.5.0
pandas==3.0.3
python-dotenv==1.2.2
httpx==0.27.2
pydantic==2.13.4
typer==0.26.8
apscheduler==3.10.4
fastapi==0.139.0
uvicorn==0.49.0
pyarrow==24.0.0

# Test dependencies (see [project.optional-dependencies].test in pyproject.toml)
pytest==9.1.1
hypothesis==6.155.7
